import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#0D0D0D',
        flex: 1,  
        alignItems: 'center',
        alignContent: 'center',
        
    }
})